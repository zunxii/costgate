"use client";
import {GitBranch,ShieldCheck,TrendingDown} from "lucide-react";
import {formatCurrency} from "@/lib/formatters";
export function RepoCard({repo,isSelected,onSelect}:{repo:any;isSelected:boolean;onSelect:()=>void}){return <button type="button" onClick={onSelect} className={`w-full text-left p-4 rounded-md border bg-white transition-all ${isSelected?"border-orange-600 ring-1 ring-orange-600/20":"border-slate-200 hover:border-slate-300"}`}><div className="flex items-center justify-between"><div className="flex items-center gap-1.5 font-mono font-bold text-slate-900 text-xs"><GitBranch className="size-3.5 text-orange-600"/>{repo.name}</div><span className="inline-flex items-center gap-1 text-[10px] font-mono text-emerald-700 bg-emerald-50 px-1.5 py-0.5 rounded border border-emerald-200"><ShieldCheck className="size-3"/>connected</span></div><div className="text-[11px] text-slate-500 mt-2">{repo.private?"Private":"Public"} · GitHub App installation</div><div className="pt-3 mt-3 border-t border-slate-100 flex items-center justify-between"><div><div className="text-[10px] uppercase font-mono text-slate-400">PRs analyzed</div><div className="text-xs font-mono font-bold">{repo.prs_analyzed}</div></div><div className="text-right"><div className="text-[10px] uppercase font-mono text-slate-400">Predicted savings</div><div className="text-xs font-mono font-bold text-emerald-600 flex items-center gap-0.5 justify-end"><TrendingDown className="size-3"/>{formatCurrency(repo.total_savings_usd)}/mo</div></div></div></button>}
