services:
  postgres:
    image: postgres:16
    container_name: costgate-postgres
    environment:
      POSTGRES_USER: costgate
      POSTGRES_PASSWORD: costgate
      POSTGRES_DB: costgate
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: